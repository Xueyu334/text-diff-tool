# 生产打包环境配置

# 部署时的基本路径 
# 注意：history 路由模式下，如果在服务器根目录，必须是 /。如果你要部署在子目录(比如 /my-app/)，则这里填 /my-app/，同时 nginx 等服务器也需要做对应转发规则。
VITE_PUBLIC_PATH=/text-diff-tool/

# 生产环境实际后端的 API 地址
VITE_API_BASE_URL=https://api.example.com
